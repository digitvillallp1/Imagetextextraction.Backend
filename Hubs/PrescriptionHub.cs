using Microsoft.AspNetCore.SignalR;

namespace Imagetextextraction.Backend.Hubs
{
    public class PrescriptionHub : Hub
    {
        // Hub can remain empty if clients only receive notifications from the server.
        // We will send notifications by calling clients directly from controllers/services.
    }
}
